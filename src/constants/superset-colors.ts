/**
 * Badge fills for the supersets in one session, handed out in order and wrapped
 * — a fifth superset is purple again. Every one of them carries white text, so
 * the light values stay dark enough to hold `accentContent`.
 */
export const SupersetColors = {
  light: ['#7A3EBF', '#B85C00', '#D0342C', '#007AFF'],
  dark: ['#8E45D8', '#C06A00', '#D64A40', '#0A84FF'],
} as const;
